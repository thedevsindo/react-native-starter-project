import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  containerStyle: {
    height: 50,
    flexDirection: 'row',
    justifyContent: 'flex-start',
    alignItems: 'center',
    borderBottomWidth: 0,
    paddingLeft: 15
  },
  labelStyle: {
    textAlign: 'left',
    color: 'white',
    fontSize: 12
  }
})
